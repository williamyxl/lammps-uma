# FC-only ngpu2 job 20899729

- **ONLY_PATHS=fc**, workers=2, after `dist.destroy_process_group` teardown fix.
- LAMMPS thermo PE = **-5830.9237 eV** on all 5 timing evals (matches ngpu1 FC / ASE).
- Reached `dist.destroy_process_group (FC)` then **hung ~2+ min on `ray.shutdown()`** (log stale).
- Job **CANCELLED** at 00:03:13 — no `parity.json` rewrite / no printed `FC E=...` line (hung inside teardown before return).
- Next: make Ray teardown non-blocking (timeout / subprocess-isolate FC path) so results are flushed before shutdown.
