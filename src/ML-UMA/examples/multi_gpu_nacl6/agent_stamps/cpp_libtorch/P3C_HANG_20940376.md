# P3c hang diagnosis — job `20940376` (CANCELLED)

**Stamp:** 2026-08-08 ~12:35 CDT  
**REPORT_OWNER=parent** (no RESULTS/SUMMARY/MULTIGPU/canvas edits)

## What happened

| Stage | Result |
|-------|--------|
| devices=1 | OK — honest **321.56 ms**, E+F green |
| devices=2 | Hung ~9+ min on gpua072 after first MP eval |
| devices=4 | never reached |

## Evidence (not mid-graph NCCL deadlock)

1. **First eval succeeded:** `PERF_PARENT` gen=1 (`ms_wait_workers≈986`), rank0 `PERF_TICK` (~982 ms compute), LAMMPS `run 0` wrote `PotEng=-5830.9237`.
2. **Rank0** logged `shutdown` then stuck in `hrtimer_nanosleep` inside `ncclCommDestroy`.
3. **Rank1** never logged shutdown; wchan=`pipe_read` (idle waiting for next cmd).
4. **Parent `lmp`** wchan=`do_wait` (`waitpid` on rank0).

## Root cause

`LibtorchMpRuntime::~LibtorchMpRuntime` did **per-rank** `write(cmd=0) → waitpid`.  
NCCL teardown requires **all** ranks to enter `ncclCommDestroy`. Rank0 entered destroy; parent blocked on `waitpid(rank0)` before sending shutdown to rank1 → **classic teardown deadlock**.

This is **not** a missing mid-step barrier on rank1 during forward (compute path completed once).

## Fix

1. Broadcast `cmd=0` to **all** workers, then `waitpid` all (bounded + `SIGKILL` fallback).
2. `release_nccl`: control-plane rendezvous before `ncclCommDestroy`.

## Campaign

**Superseded:** rebench after teardown fix → job **`20940474` PASS** — honest **321.04 / 183.30 / 112.04** (NCCL), ≤ASE/FC @1/2/4. See `perf/summary_p3c_20940474.json` and `results/RESULTS.md`.

This note remains the hang RCA for `20940376` only.
