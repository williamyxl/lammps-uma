#!/bin/bash -l
#PBS -N o5_run
#PBS -q debug
#PBS -A MatSciAI
#PBS -l filesystems=flare
#PBS -l select=1:ncpus=208
#PBS -l walltime=00:60:00
#PBS -j oe
#PBS -k doe
# opt5 tile sweep RUN: N=32 NVT 300K 10 steps on W tiles (W from WLIST). Uses the
# per-W opt2 artifacts (scripts/out/opt2/n32_w${W}; W=12 uses n32_chunk65536).
# opt3 CCL env. Reports wall/Loop/energy per W to find best throughput per tile.
module reset 2>/dev/null || true
export http_proxy=http://proxy.alcf.anl.gov:3128
export https_proxy=http://proxy.alcf.anl.gov:3128
HEN_ROOT=/lus/flare/projects/MatSciAI/xiaoliyan/workdir/hen
LU=/lus/flare/projects/MatSciAI/xiaoliyan/workdir/lammps-uma
ENG=${LU}/src/ML-UMA/uma-engine
LMP=${LU}/build-lmp-xccl/lmp
N=32; NAT=$((8*N*N*N))
OUTDIR=${LU}/scripts/out/opt5run; mkdir -p "${OUTDIR}"
source "${HEN_ROOT}/scripts/activate_fxpu.sh"
set -euo pipefail  # P3.2: strict mode (after activation)
export PYTHONPATH="${ENG}/python:${HEN_ROOT}/shim:${HEN_ROOT}/patches:${HEN_ROOT}:${PYTHONPATH:-}"
TORCH_LIB=$(python -c "import torch,os;print(os.path.join(os.path.dirname(torch.__file__),'lib'))")
ICPX_LIB=$(dirname $(dirname $(which icpx)))/lib
COMPAT=${OUTDIR}/compat; mkdir -p "${COMPAT}"; ln -sf "${CONDA_PREFIX}/lib/libsycl.so.9" "${COMPAT}/libsycl.so.8"
export LD_LIBRARY_PATH="${COMPAT}:${CONDA_PREFIX}/lib:${TORCH_LIB}:${ICPX_LIB}:${LD_LIBRARY_PATH}"
export ZE_FLAT_DEVICE_HIERARCHY=FLAT HF_HUB_OFFLINE=1 FAIRCHEM_OFFLINE=1 PYTHONUNBUFFERED=1
export UMA_CHECKPOINT="${HEN_ROOT}/uma-cache/uma-s-1p2.pt"
export CCL_PROCESS_LAUNCHER=none CCL_ATL_TRANSPORT=ofi CCL_ZE_IPC_EXCHANGE=pidfd CCL_WORKER_COUNT=1 FI_PROVIDER=tcp CCL_KVS_MODE=mpi
for W in ${WLIST:-4 6 8}; do
  if [ "${W}" = "12" ]; then ART=${LU}/scripts/out/opt2/n32_chunk65536; else ART=${LU}/scripts/out/opt2/n32_w${W}; fi
  NR=$(ls -d ${ART}/w${W}/r*/model_traced.pt 2>/dev/null|wc -l)
  if [ "${NR}" -lt "${W}" ]; then echo "W=${W}: artifacts ${NR}/${W} MISSING, skip"; continue; fi
  [ -f "${ART}/w${W}/r0/metadata.json" ] && cp "${ART}/w${W}/r0/metadata.json" "${ART}/metadata.json"
  RB=${OUTDIR}/w${W}; mkdir -p "${RB}"
  OUTDIR="${RB}" ART="${ART}" N=${N} W=1 NVT_STEPS=10 python -u "${LU}/scripts/phase6_make_gp_inputs.py" >/dev/null 2>&1
  RD=$(dirname $(ls ${RB}/*/in.sp 2>/dev/null|head -1))
  echo "=== N=32 W=${W} NVT 10 steps ==="
  t0=$SECONDS
  ( cd "${RD}" && mpiexec -n ${W} --ppn ${W} env UMA_CHECKPOINT="${UMA_CHECKPOINT}" UMA_MP_NATOMS=${NAT} LD_LIBRARY_PATH="${LD_LIBRARY_PATH}" gpu_tile_compact.sh "${LMP}" -in in.sp > lmp.log 2>&1 )
  rc=$?; WALL=$((SECONDS-t0))
  LOOP=$(grep -oE 'Loop time of [0-9.]+' ${RD}/log.lammps 2>/dev/null | grep -oE '[0-9.]+' | tail -1)
  OOM=$(grep -icE "out of memory|OUT_OF" ${RD}/lmp.log 2>/dev/null)
  PE=$(python3 - "${RD}/log.lammps" 2>/dev/null <<'PYEOF'
import re,sys
try: txt=open(sys.argv[1]).read()
except: sys.exit()
m=re.search(r"^\s*Step\b.*PotEng.*$",txt,re.M)
if not m: sys.exit()
hdr=m.group(0).split(); pe=hdr.index("PotEng"); last=None
for ln in txt[m.end():].splitlines():
    s=ln.split()
    if len(s)>=len(hdr) and s and s[0].isdigit(): last=s
if last: print(f"{float(last[pe]):.6f}")
PYEOF
)
  echo "O5_RESULT W=${W} exit=${rc} oom=${OOM} wall=${WALL}s loop=${LOOP}s step10_PE=${PE} (ref -879646.224482)"
done
echo "DONE"
